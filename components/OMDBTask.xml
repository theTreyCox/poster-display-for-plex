<?xml version="1.0" encoding="utf-8" ?>
<component name="OMDBTask" extends="Task">
    <script type="text/brightscript" uri="pkg:/components/OMDBTask.brs" />
    <interface>
        <field id="apiKey" type="string" />
        <field id="imdbId" type="string" />
        <!-- { ok, imdbRating, rottenTomatoes, metacritic, awards, error }
             Always set so the observer always fires. -->
        <field id="result" type="assocarray" alwaysNotify="true" />
    </interface>
</component>
